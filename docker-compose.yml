version: "3.9"
services:
    api:
        labels:
          dev.okteto.com/private: "true"
        build: .
        ports:
            - "8000:8000"
        volumes:
            - sqlite_volume:/eindproject/sqlitedb
        environment: 
            PORT: 8000
            FLASK_DEBUG: 1
volumes:
  sqlite_volume: